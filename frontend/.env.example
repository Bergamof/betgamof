# URL of the Kotlin API as seen from the SvelteKit server.
BETGAMOF_BACKEND_URL=http://localhost:8080
# Must match the backend's BETGAMOF_API_TOKEN.
BETGAMOF_API_TOKEN=change-me
# Password asked on the login page.
BETGAMOF_APP_PASSWORD=change-me-too
