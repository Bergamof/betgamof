build:
  maxIssues: 0

style:
  MaxLineLength:
    # Same limit as ktlint (ktlint_official code style).
    maxLineLength: 140
  MagicNumber:
    ignoreEnums: true
    ignorePropertyDeclaration: true
    ignoreNumbers: ['-1', '0', '1', '2', '100', '100.0']
    # Demo seed (app/seed) and simulated odds catalog (adapters/odds) are fixture data.
    excludes: ['**/test/**', '**/seed/**', '**/adapter/odds/**']

complexity:
  LongParameterList:
    excludes: ['**/test/**', '**/seed/**', '**/adapter/odds/**']
  LongMethod:
    excludes: ['**/adapter/odds/**']
  TooManyFunctions:
    excludes: ['**/test/**', '**/seed/**']
